# Ensure a**
   - Confirm there is a step to build your static build step exists before archiving artifacts
- name: Build content before archiving ./public.
   - Example:
 project
  run: npm run build

# Archive the correct directory     ```yaml
     - name: Build static site
       run: npm run build

     - name: Archive artifact

- name: Archive artifact
  run: tar -cvf public.tar ./public
